PORT=7001
# No server-side API key needed — each user provides their own
# OpenSubtitles API key via the add-on configuration UI in Stremio.
# Get a free key at: opensubtitles.com → My Profile → API Access
